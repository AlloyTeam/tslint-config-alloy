{
    "rules": {
        "prefer-method-signature": false
    },
    "meta": {
        "category": "style",
        "description": "必须使用 `foo(): void` 而不是 `foo: () => void`",
        "reason": "没必要限制",
        "has-fixer": true
    }
}
